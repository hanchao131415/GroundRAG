# GroundRAG Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Turn `project1-rag` into a GitHub showcase project `GroundRAG` — renamed, in a new clean repo, with a React+Vite frontend that demos streaming chat, source citations, RBAC user-switching, a trace panel, search mode, and stats.

**Architecture:** Monorepo. FastAPI backend (minimal changes) serves both `/api/*` and the built Vite frontend (`web/dist/`). Dev = two processes (uvicorn + vite proxy); prod/review = one command. A new `EnterpriseRAGSystem.ask_stream()` unifies streaming + non-streaming chat through one traced, source-emitting path (also fixes the existing stream/`ask()` divergence).

**Tech Stack:** Python 3.12, FastAPI, FAISS, LangChain (existing backend); React 18 + Vite 5 + TypeScript + Tailwind CSS (new frontend); Vitest + React Testing Library (frontend tests); pytest (existing + new backend tests).

## Global Constraints

- **OS/Shell:** Windows, PowerShell primary (Bash tool also available). Use cross-platform `python -m venv`, `npm`. For file copy use PowerShell `Copy-Item -Recurse`.
- **New repo location:** `F:\code\python\GroundRAG\` (sibling to `agent-platform`). Fresh `git init` — do NOT copy old `.git/`.
- **No secrets:** Never copy `project1-rag/.env` (real API keys). Provide `.env.example` with placeholders only.
- **No real company docs:** Replace `data/docs/mye/` etc. with synthetic sample docs (fictional company). Keep department structure (HR/财务/IT/行政/研发).
- **Python:** `>=3.12,<3.14`. **Node:** `>=18`. **transformers==4.46.3** pinned (per existing requirements).
- **Naming:** package `groundrag`; UI title/README h1 `GroundRAG`.
- **TDD:** Every logic task writes the failing test first, runs it (fails), implements, runs it (passes), commits.
- **Frequent commits:** Commit at the end of every task.

---

## File Structure

**New repo root `F:\code\python\GroundRAG\`:**

Backend (copied from project1-rag, then modified):
- `app/api.py` — MODIFY: new `/auth/demo-users`, typed-SSE `/chat`, `ChatResponse` fields, static serving + SPA fallback.
- `app/auth.py` — copied unchanged.
- `main.py` — MODIFY: add `EnterpriseRAGSystem.ask_stream()` + helpers `_source_item`, `_trace_public`.
- `rag_modules/*`, `config.py`, `evaluation/*`, `locustfile.py`, `pyproject.toml`, `requirements.txt` — copied; `pyproject.toml` name→`groundrag`.
- `tests/test_api_streaming.py` — NEW: SSE event sequence + sources + trace + demo-users.
- `tests/test_core.py`, `tests/test_rbac_subindex.py` — copied.

Data:
- `data/users.json` — copied.
- `data/docs/{HR,财务,IT,行政,研发}/*.{md,txt,xlsx,docx,pdf}` — NEW synthetic sample docs, **multiple formats per department** (md/txt/xlsx/docx/pdf mixed across all 5 depts), generated by `scripts/make_sample_docs.py`.
- `data/cache/`, `data/traces/`, `data/vector_index/` — gitignored runtime artifacts.
- `scripts/make_sample_docs.py` — generates the synthetic multi-format sample docs.

Frontend (NEW):
- `web/package.json`, `web/vite.config.ts`, `web/tsconfig.json`, `web/tailwind.config.js`, `web/postcss.config.js`, `web/index.html`
- `web/src/main.tsx`, `web/src/App.tsx`, `web/src/styles/index.css`
- `web/src/types.ts` — shared TS types.
- `web/src/api/client.ts` — fetch wrapper + JWT.
- `web/src/api/sse.ts` — pure SSE chunk parser (tested).
- `web/src/hooks/useAuth.ts`, `web/src/hooks/useSSEChat.ts`
- `web/src/i18n/index.ts` — lightweight zh/en i18n (`LangProvider`, `useLang`, `t`, complete dictionaries).
- `web/src/components/UserSwitcher.tsx`, `ChatWindow.tsx`, `MessageBubble.tsx`, `SourceCard.tsx`, `TracePanel.tsx`, `SearchResults.tsx`
- `web/src/views/ChatView.tsx`, `SearchView.tsx`, `StatsView.tsx`
- `web/src/api/__tests__/sse.test.ts` — SSE parser tests.

Repo meta:
- `README.md`, `README.zh-CN.md` — NEW bilingual.
- `LICENSE` (MIT), `Makefile`, `.env.example`, `.gitignore`.
- `Dockerfile` (multi-stage), `docker-compose.yml`, `.dockerignore`, `docs/deployment.md`.

---

# Phase 0 — Scaffold & migrate

### Task 0.1: Create GroundRAG repo skeleton + copy backend

**Files:** Create `F:\code\python\GroundRAG\` and copy backend sources into it.

- [ ] **Step 1: Create the new repo directory and copy backend**

PowerShell:
```powershell
New-Item -ItemType Directory -Force -Path F:\code\python\GroundRAG
$src = "F:\code\python\agent-platform\project1-rag"
$dst = "F:\code\python\GroundRAG"
# Backend code + tests + eval + docs
Copy-Item -Recurse "$src\app" "$dst\app"
Copy-Item -Recurse "$src\rag_modules" "$dst\rag_modules"
Copy-Item "$src\config.py" "$dst\config.py"
Copy-Item "$src\main.py" "$dst\main.py"
Copy-Item -Recurse "$src\tests" "$dst\tests"
Copy-Item -Recurse "$src\evaluation" "$dst\evaluation"
Copy-Item "$src\locustfile.py" "$dst\locustfile.py"
Copy-Item "$src\pyproject.toml" "$dst\pyproject.toml"
Copy-Item "$src\requirements.txt" "$dst\requirements.txt"
Copy-Item -Recurse "$src\docs" "$dst\docs"
# Users
New-Item -ItemType Directory -Force -Path "$dst\data"
Copy-Item "$src\data\users.json" "$dst\data\users.json"
```

- [ ] **Step 2: Do NOT copy secrets / runtime / personal content**

Verify these were NOT copied (they must not exist in `GroundRAG\`):
```powershell
Test-Path F:\code\python\GroundRAG\.env         # must be False
Test-Path F:\code\python\GroundRAG\.venv        # must be False
Test-Path F:\code\python\GroundRAG\.git         # must be False
Test-Path F:\code\python\GroundRAG\data\vector_index  # must be False
```
Expected: all `False`.

- [ ] **Step 3: Write `.gitignore`**

Create `F:\code\python\GroundRAG\.gitignore`:
```gitignore
# Python
__pycache__/
*.py[cod]
*.egg-info/
.Python

# Virtual env
.venv/
venv/

# Env / secrets
.env
.env.local
*.key
*.secret

# IDE
.idea/
.vscode/

# Runtime data
data/cache/
data/traces/
data/vector_index/
*.pkl
*.faiss

# Frontend build
web/dist/
web/node_modules/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
.pytest_cache/
```

- [ ] **Step 4: Init git + rename package**

```powershell
cd F:\code\python\GroundRAG
git init
git add .gitignore
git commit -m "chore: init GroundRAG repo (gitignore)"
```

- [ ] **Step 5: Rename package in `pyproject.toml`**

Edit `F:\code\python\GroundRAG\pyproject.toml`:
```toml
[project]
name = "groundrag"
version = "1.0.0"
description = "GroundRAG — 企业级 RAG 知识库展示项目：混合检索 + RBAC 权限 + 多级缓存 + 全链路可观测 + React 前端"
```
Rest of file unchanged.

```powershell
cd F:\code\python\GroundRAG
git add pyproject.toml
git commit -m "chore: rename package to groundrag"
```

---

### Task 0.2: 合成样例文档（多格式）+ `.env.example`

**Files:** Create `scripts/make_sample_docs.py`（生成 md/txt/xlsx/docx/pdf 五种格式样例，覆盖 5 个部门）；`data/users.json`（已拷贝）；`.env.example`。脚本依赖均为后端已有依赖（pandas/openpyxl、python-docx、PyMuPDF/fitz）；PDF 用 fitz 内置 CJK 字体 `china-s`，无需额外字体文件。

- [ ] **Step 1: 写 `scripts/make_sample_docs.py`**

`F:\code\python\GroundRAG\scripts\make_sample_docs.py`:
```python
"""生成 GroundRAG 合成样例知识库，覆盖 md/txt/xlsx/docx/pdf 五种格式。

运行：python scripts/make_sample_docs.py
依赖：pandas, openpyxl, python-docx, PyMuPDF(fitz) —— 均为后端已有依赖。
PDF 用 fitz 内置 CJK 字体 china-s，无需额外字体文件。
"""
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent / "data" / "docs"


def write_text(rel, content):
    p = ROOT / rel
    p.parent.mkdir(parents=True, exist_ok=True)
    p.write_text(content, encoding="utf-8")


def write_xlsx(rel, header, rows, sheet="Sheet1"):
    import pandas as pd
    p = ROOT / rel
    p.parent.mkdir(parents=True, exist_ok=True)
    pd.DataFrame(rows, columns=header).to_excel(p, sheet_name=sheet, index=False)


def write_docx(rel, title, paragraphs):
    from docx import Document
    p = ROOT / rel
    p.parent.mkdir(parents=True, exist_ok=True)
    d = Document()
    d.add_heading(title, level=1)
    for para in paragraphs:
        d.add_paragraph(para)
    d.save(str(p))


def write_pdf(rel, title, lines):
    import fitz  # PyMuPDF，内置 CJK 字体 china-s
    p = ROOT / rel
    p.parent.mkdir(parents=True, exist_ok=True)
    doc = fitz.open()
    page = doc.new_page()
    y = 60
    page.insert_text((72, y), title, fontname="china-s", fontsize=18)
    y += 30
    for ln in lines:
        page.insert_text((72, y), ln, fontname="china-s", fontsize=12)
        y += 20
    doc.save(str(p))
    doc.close()


if __name__ == "__main__":
    # ===== HR：md + txt + pdf =====
    write_text("HR/年假管理制度.md",
               "# 示例科技有限公司 年假管理制度\n\n## 年假天数标准\n"
               "- 工作满1年不满3年：年假5天\n- 工作满3年不满5年：年假10天\n"
               "- 工作满5年不满10年：年假15天\n- 工作满10年以上：年假20天\n\n"
               "## 请假流程\n请假须提前通过OA系统提交申请，由直属主管审批。年假可按半天为单位使用。\n")
    write_text("HR/考勤管理制度.txt",
               "示例科技有限公司 考勤管理制度\n\n"
               "工作时间：9:00-18:00，午休 12:00-13:00。\n"
               "打卡规则：每日上班、下班各打卡一次。迟到超过30分钟按半天事假处理。病假须提供医院证明。\n")
    write_pdf("HR/薪酬福利管理办法.pdf", "薪酬福利管理办法",
              ["工资每月10日发放，遇节假日顺延。",
               "年终奖根据公司业绩与个人考核发放。",
               "五险一金按国家规定缴纳。"])

    # ===== 财务：xlsx + docx =====
    write_xlsx("财务/差旅报销标准.xlsx",
               header=["城市级别", "住宿费上限(元/晚)", "市内交通补贴(元/天)"],
               rows=[["一线城市", 500, 80], ["二线城市", 400, 60], ["三线及以下", 300, 50]])
    write_docx("财务/报销管理制度.docx", "报销管理制度",
               ["所有报销须在费用发生后30日内提交。",
                "单笔超过5000元的报销须部门负责人与财务总监双签。",
                "发票须真实、合法，与业务一致。"])

    # ===== IT：pdf + md =====
    write_pdf("IT/信息安全管理规定.pdf", "信息安全管理规定",
              ["密码长度至少8位，须包含大小写字母与数字。",
               "密码每90天更换一次。",
               "连续5次输错密码将锁定账号30分钟。",
               "禁止将公司内网账号借予他人。离职时须立即收回所有系统权限。"])
    write_text("IT/网络管理规范.md",
               "# 示例科技有限公司 网络管理规范\n\n"
               "## 办公网络\n办公网络与访客网络物理隔离。禁止私自接入路由器或随身WiFi。\n\n"
               "## VPN\n远程办公须通过公司VPN接入，VPN账号禁止共享。\n")

    # ===== 行政：docx + xlsx =====
    write_docx("行政/办公用品领用管理办法.docx", "办公用品领用管理办法",
               ["办公用品统一由行政部采购。员工通过OA系统提交领用申请，主管审批后到行政部领取。",
                "每人每月办公用品预算不超过200元。特殊用品（如人体工学椅）须部门负责人额外审批。"])
    write_xlsx("行政/采购分级标准.xlsx",
               header=["采购金额(元)", "审批层级", "是否需招标"],
               rows=[["<1000", "部门主管", "否"],
                     ["1000-10000", "部门负责人", "否"],
                     [">10000", "总经理", "是"]])

    # ===== 研发：md + txt =====
    write_text("研发/代码规范.md",
               "# 示例科技有限公司 研发代码规范\n\n## 代码评审\n"
               "所有代码合并前须经至少一位同事评审。主分支禁止直接 push。\n\n"
               "## 分支命名\n功能分支：feature/简短描述。修复分支：fix/简短描述。发布分支：release/版本号。\n")
    write_text("研发/开发流程.txt",
               "示例科技有限公司 研发开发流程\n\n"
               "需求评审 → 技术方案 → 编码 → 代码评审 → 测试 → 灰度发布 → 全量发布。\n"
               "每个环节须有产出物并存档。线上发布须双人复核。\n")

    print(f"sample docs written under {ROOT}")
```

> 脚本在 Task 0.3 装好依赖后运行（需 pandas/docx/fitz），生成 `data/docs/` 覆盖 5 部门、**每部门多种格式**（md/txt/xlsx/docx/pdf 混合，共约 11 个文件），演示后端多格式解析（PDF/DOCX/XLSX/MD/TXT）。

- [ ] **Step 2: Write `.env.example`**

`F:\code\python\GroundRAG\.env.example`:
```env
# ===== LLM（必填，二选一）=====
# DeepSeek 示例
LLM_PROVIDER=deepseek
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_API_KEY=sk-your-key-here
LLM_MODEL=deepseek-chat
# 或 智谱 GLM：
# LLM_PROVIDER=zhipu
# LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4/
# LLM_MODEL=glm-4-flash

LLM_TEMPERATURE=0.1
LLM_MAX_TOKENS=2048

# ===== 文档处理 =====
RAG_CHUNK_SIZE=500
RAG_CHUNK_OVERLAP=25

# ===== 检索 =====
RAG_VECTOR_K=15
RAG_BM25_K=15
RAG_TOP_K=3
RAG_RRF_K=60

# ===== JWT（生产必须改成随机长串）=====
JWT_SECRET=change-me-to-a-random-long-string
JWT_EXPIRE_HOURS=24

# ===== 可观测（可选，不配则只用本地 trace）=====
# LANGFUSE_PUBLIC_KEY=
# LANGFUSE_SECRET_KEY=
# LANGFUSE_HOST=https://cloud.langfuse.com
```

- [ ] **Step 3: Commit**

```powershell
cd F:\code\python\GroundRAG
git add scripts data/users.json .env.example
git commit -m "chore: sample-doc generator (md/txt/xlsx/docx/pdf) + .env.example"
```

---

### Task 0.3: Backend smoke test (verify copy runs)

**Goal:** Confirm the copied backend imports and builds the index with synthetic docs.

- [ ] **Step 1: Create venv + install deps + 生成多格式样例文档**

```powershell
cd F:\code\python\GroundRAG
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -U pip
pip install -r requirements.txt
python scripts/make_sample_docs.py
```
Note: `transformers==4.46.3` must install last if pip tries to upgrade it (per requirements.txt header). The last command runs the Task 0.2 generator → `data/docs/{HR,财务,IT,行政,研发}/*.{md,txt,xlsx,docx,pdf}`.

- [ ] **Step 2: Create `.env` from example + set a real key**

```powershell
Copy-Item .env.example .env
```
Edit `.env` and fill `LLM_API_KEY` with a real key (local only — `.env` is gitignored). This is required for any generation; retrieval-only smoke can skip it.

- [ ] **Step 3: Smoke run — build knowledge base**

```powershell
cd F:\code\python\GroundRAG
.\.venv\Scripts\Activate.ps1
python main.py
```
Expected: logs show "成功加载 N 个文档片段", "切分完成", builds FAISS index, then prompts for a user ID. Type `admin`, then ask "工作满3年年假几天" — expect an answer citing `HR/年假管理制度.md`. Type `退出`.

- [ ] **Step 4: Commit generated sample docs**

```powershell
cd F:\code\python\GroundRAG
git add data/docs
git commit -m "chore: generated multi-format sample docs (md/txt/xlsx/docx/pdf)"
```
If the smoke failed, fix before committing.

---

# Phase 1 — Backend API extensions

### Task 1.1: `GET /auth/demo-users` (TDD)

**Files:**
- Modify: `app/api.py` (add endpoint)
- Test: `tests/test_api_streaming.py` (new file)

**Interfaces:**
- Produces: `GET /auth/demo-users` → `{"users":[{"user_id","name","departments","role"}]}`.

- [ ] **Step 1: Write failing test**

`F:\code\python\GroundRAG\tests\test_api_streaming.py`:
```python
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from fastapi.testclient import TestClient


def _client(monkeypatch=None):
    from app import api
    class FakeUserService:
        users = {
            "zhangsan": {"name": "张三", "departments": ["HR"], "role": "员工"},
            "admin": {"name": "管理员", "departments": ["*"], "role": "管理员"},
        }
        def authenticate(self, uid): return self.users.get(uid)
        def get_departments(self, uid): return self.users[uid]["departments"]
    class FakeRag:
        user_service = FakeUserService()
    api._rag = FakeRag()
    return TestClient(api.app)


def test_demo_users_returns_list():
    client = _client()
    resp = client.get("/auth/demo-users")
    assert resp.status_code == 200
    data = resp.json()
    uids = {u["user_id"] for u in data["users"]}
    assert "zhangsan" in uids and "admin" in uids
    zs = next(u for u in data["users"] if u["user_id"] == "zhangsan")
    assert zs["name"] == "张三" and zs["departments"] == ["HR"] and zs["role"] == "员工"
```

- [ ] **Step 2: Run — verify fail**

```powershell
cd F:\code\python\GroundRAG
.\.venv\Scripts\Activate.ps1
pytest tests/test_api_streaming.py::test_demo_users_returns_list -v
```
Expected: FAIL (404 — route doesn't exist yet).

- [ ] **Step 3: Implement endpoint**

In `app/api.py`, add after the `login` endpoint (before `/chat`):
```python
@app.get("/auth/demo-users")
def demo_users():
    """列出 demo 用户，供前端下拉框渲染（公开：均为无密码 demo 账号）。"""
    rag = get_rag()
    users = []
    for uid, info in rag.user_service.users.items():
        users.append({
            "user_id": uid,
            "name": info.get("name", uid),
            "departments": info.get("departments", []),
            "role": info.get("role", ""),
        })
    return {"users": users}
```

- [ ] **Step 4: Run — verify pass**

```powershell
pytest tests/test_api_streaming.py::test_demo_users_returns_list -v
```
Expected: PASS.

- [ ] **Step 5: Commit**

```powershell
git add app/api.py tests/test_api_streaming.py
git commit -m "feat(api): add GET /auth/demo-users"
```

---

### Task 1.2: `EnterpriseRAGSystem.ask_stream()` — typed-event generator (TDD)

**Files:**
- Modify: `main.py` (add `ask_stream`, `_source_item`, `_trace_public`)
- Test: `tests/test_api_streaming.py` (append)

**Interfaces:**
- Produces: `EnterpriseRAGSystem.ask_stream(question: str, user_departments=None, user_id=None) -> Generator[dict]` yielding event dicts:
  - `{"type":"sources","items":[Source]}` (Source = `{source,page,department,score,preview}`)
  - `{"type":"token","text":str}`
  - `{"type":"trace","trace":Trace}` (Trace = `{trace_id,steps:[{name,ms,tokens?}],total_ms,tokens:{prompt,completion,total},cost_usd}`)
  - `{"type":"error","message":str}`
  - `{"type":"done"}`
- Consumes: existing `generation_module.generate_answer_stream_with_usage`, `retrieval_module.permission_aware_search`/`hybrid_search`, `cache_service.is_simple_query`, `tracer.Tracer`.

- [ ] **Step 1: Write failing test**

Append to `tests/test_api_streaming.py`:
```python
def test_ask_stream_emits_typed_events_in_order():
    """ask_stream 必须按序发 sources→token(s)→trace→done，且 trace 含各步。"""
    from main import EnterpriseRAGSystem
    from langchain_core.documents import Document

    sys_obj = EnterpriseRAGSystem.__new__(EnterpriseRAGSystem)
    sys_obj.config = type("C", (), {"llm_provider": "deepseek", "top_k": 3})()
    sys_obj.cache = None
    sys_obj.current_user = None

    fake_chunk = Document(page_content="工作满3年年假10天。", metadata={
        "source": "HR/年假管理制度.md", "page": 1, "department": "HR", "vector_sim": 0.9})

    class FakeRet:
        def permission_aware_search(self, q, deps, top_k): return [fake_chunk]
        def hybrid_search(self, q, top_k): return [fake_chunk]
    class FakeGen:
        ANSWER = "工作满3年年假10天。"
        def query_router(self, q): return "retrieval"
        def query_rewrite(self, q): return q
        def generate_answer_stream_with_usage(self, q, chunks):
            yield self.ANSWER[:6], None
            yield self.ANSWER[6:], {"prompt": 10, "completion": 5, "total": 15}
    sys_obj.retrieval_module = FakeRet()
    sys_obj.generation_module = FakeGen()

    events = list(sys_obj.ask_stream("工作满3年年假几天", user_departments=["HR"], user_id="zhangsan"))
    types = [e["type"] for e in events]

    assert types[0] == "sources"                      # sources first
    assert types[-1] == "done"                         # done last
    assert "trace" in types                            # trace present
    assert types.index("sources") < types.index("trace") < types.index("done")
    # sources content
    src = next(e for e in events if e["type"] == "sources")["items"][0]
    assert src["source"] == "HR/年假管理制度.md" and src["department"] == "HR"
    # tokens concatenate to the answer
    answer = "".join(e["text"] for e in events if e["type"] == "token")
    assert answer == "工作满3年年假10天。"
    # trace has steps + tokens + cost
    tr = next(e for e in events if e["type"] == "trace")["trace"]
    assert tr["trace_id"] and tr["total_ms"] >= 0
    assert tr["tokens"]["total"] == 15
    assert any(s["name"] == "检索" for s in tr["steps"])
    assert tr["cost_usd"] >= 0


def test_ask_stream_no_result_refuses():
    """检索为空时返回拒答 token + trace + done。"""
    from main import EnterpriseRAGSystem
    sys_obj = EnterpriseRAGSystem.__new__(EnterpriseRAGSystem)
    sys_obj.config = type("C", (), {"llm_provider": "deepseek", "top_k": 3})()
    sys_obj.cache = None
    sys_obj.current_user = None
    class FakeRet:
        def hybrid_search(self, q, top_k): return []
    class FakeGen:
        def query_router(self, q): return "retrieval"
        def query_rewrite(self, q): return q
    sys_obj.retrieval_module = FakeRet()
    sys_obj.generation_module = FakeGen()

    events = list(sys_obj.ask_stream("无关问题", user_departments=None))
    types = [e["type"] for e in events]
    assert types[-1] == "done"
    answer = "".join(e["text"] for e in events if e["type"] == "token")
    assert "未找到" in answer
```

- [ ] **Step 2: Run — verify fail**

```powershell
pytest tests/test_api_streaming.py::test_ask_stream_emits_typed_events_in_order -v
```
Expected: FAIL (`AttributeError: ask_stream`).

- [ ] **Step 3: 加共享决策 helper + `ask_stream`（用 helper）**

向 `EnterpriseRAGSystem` 加三个 helper（`ask()` 与 `ask_stream()` 共用，单一事实源）+ `ask_stream` 事件生成器：

```python
    # ===== 共享决策 helper（ask 与 ask_stream 共用，单一事实源）=====
    def _decide_intent(self, question: str):
        """返回 (intent, elapsed_ms)。明确问题短路，省 ~20s LLM 调用。"""
        from rag_modules.tracer import elapsed_ms, time_it
        from rag_modules.cache_service import is_simple_query
        t0 = time_it()
        intent = "retrieval" if is_simple_query(question) else self.generation_module.query_router(question)
        return intent, elapsed_ms(t0)

    def _rewrite_query(self, question: str):
        """返回 (rewritten, elapsed_ms)。明确问题短路，省 ~6s。"""
        from rag_modules.tracer import elapsed_ms, time_it
        from rag_modules.cache_service import is_simple_query
        t0 = time_it()
        rewritten = question if is_simple_query(question) else self.generation_module.query_rewrite(question)
        return rewritten, elapsed_ms(t0)

    def _retrieve(self, rewritten: str, user_departments):
        """返回 (chunks, elapsed_ms)。有部门→permission_aware_search，否则 hybrid_search。"""
        from rag_modules.tracer import elapsed_ms, time_it
        t0 = time_it()
        if user_departments is not None:
            chunks = self.retrieval_module.permission_aware_search(rewritten, user_departments, top_k=self.config.top_k)
        else:
            chunks = self.retrieval_module.hybrid_search(rewritten, top_k=self.config.top_k)
        return chunks, elapsed_ms(t0)

    def ask_stream(self, question: str, user_departments=None, user_id: str = None):
        """流式问答，产出类型化事件 dict 序列（供 API 转 SSE）。

        事件序列：sources → token(若干) → trace → done（异常时插 error）。
        这条路径补齐了原 streaming 分支缺失的 trace/token 统计，
        并与非流式 ask() 对齐。
        """
        from rag_modules.tracer import Tracer, elapsed_ms, time_it

        if user_departments is None:
            user_departments = self._current_departments()

        tracer = Tracer()
        effective_user = user_id or self.current_user
        tr = tracer.start(question, effective_user)

        def emit_trace():
            tracer.finish(tr, "", provider=self.config.llm_provider)  # finalize cost
            return self._trace_public(tr)

        try:
            # 缓存命中短路
            if self.cache:
                t0 = time_it()
                hit = self.cache.get(question, departments=user_departments)
                if hit:
                    tracer.step(tr, "缓存命中", question, hit["cache_type"], elapsed_ms(t0))
                    yield {"type": "sources", "items": []}
                    yield {"type": "token", "text": hit["answer"]}
                    tracer.finish(tr, hit["answer"], provider=self.config.llm_provider)
                    yield {"type": "trace", "trace": self._trace_public(tr)}
                    yield {"type": "done"}
                    return

            intent, ms = self._decide_intent(question)
            tracer.step(tr, "意图路由", question, intent, ms)
            if intent == "chitchat":
                yield {"type": "sources", "items": []}
                yield {"type": "token", "text": "(闲聊，不检索)"}
                tracer.finish(tr, "(闲聊)", provider=self.config.llm_provider)
                yield {"type": "trace", "trace": self._trace_public(tr)}
                yield {"type": "done"}
                return

            rewritten, ms = self._rewrite_query(question)
            tracer.step(tr, "查询改写", question, rewritten, ms)

            chunks, ms = self._retrieve(rewritten, user_departments)
            sources = [self._source_item(c) for c in chunks]
            tracer.step(tr, "检索", rewritten, sources, ms)
            yield {"type": "sources", "items": sources}

            if not chunks:
                msg = "知识库中未找到相关内容"
                yield {"type": "token", "text": msg}
                tracer.finish(tr, msg, status="no_result", provider=self.config.llm_provider)
                yield {"type": "trace", "trace": self._trace_public(tr)}
                yield {"type": "done"}
                return

            # 流式生成 + usage
            t0 = time_it()
            usage = None
            collected = ""
            for piece, u in self.generation_module.generate_answer_stream_with_usage(question, chunks):
                collected += piece
                yield {"type": "token", "text": piece}
                if u:
                    usage = u
            if not usage:
                est = max(1, len(collected) // 2)
                usage = {"prompt": est, "completion": est, "total": est * 2}
            tracer.step(tr, "生成", question, collected, elapsed_ms(t0), usage)

            if self.cache and collected:
                self.cache.put(question, collected, departments=user_departments)

            tracer.finish(tr, collected, provider=self.config.llm_provider)
            yield {"type": "trace", "trace": self._trace_public(tr)}
            yield {"type": "done"}

        except Exception as e:
            logger.error(f"ask_stream 出错: {e}")
            tracer.finish(tr, "", status="error", error=str(e), provider=self.config.llm_provider)
            yield {"type": "error", "message": str(e)}
            yield {"type": "trace", "trace": self._trace_public(tr)}
            yield {"type": "done"}

    @staticmethod
    def _source_item(c) -> dict:
        md = c.metadata
        return {
            "source": md.get("source", ""),
            "page": md.get("page"),
            "department": md.get("department", ""),
            "score": md.get("rerank_score") or md.get("vector_sim", 0),
            "preview": c.page_content[:200],
        }

    @staticmethod
    def _trace_public(tr) -> dict:
        return {
            "trace_id": tr["trace_id"],
            "steps": [{"name": s["name"], "ms": s["ms"], "tokens": s.get("tokens")} for s in tr["steps"]],
            "total_ms": tr["total_ms"],
            "tokens": tr["tokens"],
            "cost_usd": tr["cost_usd"],
        }
```

- [ ] **Step 3b: 重构 `ask()` 改用同一组 helper（P0/DRY）**

把 `main.py` 现有 `ask()` 里的"意图路由 / 查询改写 / 检索"三段**内联逻辑**替换为对 helper 的调用——决策逻辑只留一份实现，`ask()` 与 `ask_stream()` 不再各写一套。`ask()` 的打印 / trace 汇总逻辑保持不变，只换决策段：

```python
# 意图段（删掉旧的 is_simple_query/query_router 内联，改为）：
intent, ms = self._decide_intent(question)
tracer.step(tr, "意图路由", question, intent, ms)

# 改写段（删掉旧的内联，改为）：
rewritten, ms = self._rewrite_query(question)
tracer.step(tr, "查询改写", question, rewritten, ms)

# 检索段（删掉旧的 permission_aware_search/hybrid_search 分支内联，改为）：
chunks, ms = self._retrieve(rewritten, user_departments)
sources = [c.metadata.get("source", "?") for c in chunks]
tracer.step(tr, "检索", rewritten, sources, ms)
```

> 这样 `ask()`（CLI）与 `ask_stream()`（API）的路由/改写/检索只有一份实现；后续修 bug 改 helper 即可，两条管线同步。CLI 行为（`run_interactive` / `demo_rbac`）不变。

- [ ] **Step 4: Run — verify pass**

```powershell
pytest tests/test_api_streaming.py -v
```
Expected: all 3 tests PASS (demo-users + 2 ask_stream).

- [ ] **Step 5: Commit**

```powershell
git add main.py tests/test_api_streaming.py
git commit -m "feat(core): add ask_stream typed-event generator"
```

---

### Task 1.3: Wire `/chat` to typed SSE + new `ChatResponse` (TDD)

**Files:**
- Modify: `app/api.py` (`ChatResponse` fields, `chat()` stream + non-stream branches)

**Interfaces:**
- Produces: `POST /api/v1/chat` (stream) emits `data: {json}\n\n` events then `data: [DONE]\n\n`; (non-stream) returns `{answer, sources, trace, trace_id}`.

- [ ] **Step 1: Write failing test**

Append to `tests/test_api_streaming.py`:
```python
def test_chat_non_stream_returns_sources_and_trace():
    """非流式 /chat 返回真实 sources + trace（不再空）。"""
    from app import api
    from main import EnterpriseRAGSystem
    from langchain_core.documents import Document

    class FakeUserService:
        users = {"zhangsan": {"name": "张三", "departments": ["HR"], "role": "员工"}}
        def authenticate(self, uid): return self.users.get(uid)
        def get_departments(self, uid): return self.users[uid]["departments"]

    class FakeRag(EnterpriseRAGSystem):
        def __init__(self):
            self.user_service = FakeUserService()
            self.config = type("C", (), {"top_k": 3, "jwt_secret": "x"})()
        def ask_stream(self, question, user_departments=None, user_id=None):
            yield {"type": "sources", "items": [{"source": "HR/x.md", "page": 1, "department": "HR", "score": 0.9, "preview": "p"}]}
            yield {"type": "token", "text": "答案"}
            yield {"type": "trace", "trace": {"trace_id": "abc", "steps": [], "total_ms": 5,
                                              "tokens": {"prompt": 1, "completion": 1, "total": 2}, "cost_usd": 0.0}}
            yield {"type": "done"}

    api._rag = FakeRag()
    client = TestClient(api.app)
    token = client.post("/auth/login", json={"user_id": "zhangsan"}).json()["access_token"]
    resp = client.post("/api/v1/chat", json={"question": "q", "stream": False},
                       headers={"Authorization": f"Bearer {token}"})
    assert resp.status_code == 200
    body = resp.json()
    assert body["answer"] == "答案"
    assert body["sources"][0]["source"] == "HR/x.md"
    assert body["trace"]["trace_id"] == "abc"
    assert body["trace_id"] == "abc"


def test_chat_stream_emits_sse_frames():
    from app import api
    from main import EnterpriseRAGSystem

    class FakeUserService:
        users = {"zhangsan": {"name": "张三", "departments": ["HR"], "role": "员工"}}
        def authenticate(self, uid): return self.users.get(uid)
        def get_departments(self, uid): return self.users[uid]["departments"]
    class FakeRag(EnterpriseRAGSystem):
        def __init__(self):
            self.user_service = FakeUserService()
            self.config = type("C", (), {"top_k": 3, "jwt_secret": "x"})()
        def ask_stream(self, question, user_departments=None, user_id=None):
            yield {"type": "sources", "items": []}
            yield {"type": "token", "text": "hi"}
            yield {"type": "done"}

    api._rag = FakeRag()
    client = TestClient(api.app)
    token = client.post("/auth/login", json={"user_id": "zhangsan"}).json()["access_token"]
    resp = client.post("/api/v1/chat", json={"question": "q", "stream": True},
                       headers={"Authorization": f"Bearer {token}"})
    body = resp.text
    assert "data:" in body
    assert '"type": "sources"' in body or '"type":"sources"' in body
    assert '"type": "token"' in body or '"type":"token"' in body
    assert "data: [DONE]" in body
```

- [ ] **Step 2: Run — verify fail**

```powershell
pytest tests/test_api_streaming.py::test_chat_non_stream_returns_sources_and_trace -v
```
Expected: FAIL (sources empty / trace missing).

- [ ] **Step 3: Update `ChatResponse` + `chat()` in `app/api.py`**

Replace the existing `ChatResponse` class:
```python
class ChatResponse(BaseModel):
    """非流式问答返回。"""
    answer: str
    sources: list[dict] = []
    trace: dict = {}
    trace_id: str = ""
```

Replace the entire `chat()` function body with:
```python
@app.post("/api/v1/chat")
@limiter.limit("20/minute")
async def chat(req: ChatRequest, request: Request, user_id: str = Depends(get_current_user)):
    """问答（需 JWT）。流式走 SSE 类型化事件；非流式一次性返回 answer+sources+trace。"""
    rag = get_rag()
    deps = _resolve_departments(rag, user_id)

    if req.stream:
        import json, asyncio, threading
        async def generate():
            # P1：ask_stream 是同步生成器（含阻塞 LLM/检索/向量化 I/O），放后台线程，
            # 经 asyncio.Queue + run_coroutine_threadsafe 桥到 async 生成器，
            # 避免阻塞事件循环（否则并发请求会串行）。ask_stream 自身的异常已封装成 error 事件。
            q: asyncio.Queue = asyncio.Queue()
            loop = asyncio.get_running_loop()

            def producer():
                try:
                    for event in rag.ask_stream(req.question, user_departments=deps, user_id=user_id):
                        asyncio.run_coroutine_threadsafe(q.put(event), loop)
                except Exception as e:  # 线程内未预期异常兜底
                    asyncio.run_coroutine_threadsafe(q.put({"type": "error", "message": str(e)}), loop)
                finally:
                    asyncio.run_coroutine_threadsafe(q.put(None), loop)  # 结束哨兵

            threading.Thread(target=producer, daemon=True).start()
            while True:
                event = await q.get()
                if event is None:
                    break
                yield "data: [DONE]\n\n" if event["type"] == "done" \
                    else f"data: {json.dumps(event, ensure_ascii=False)}\n\n"
        return StreamingResponse(generate(), media_type="text/event-stream")

    # 非流式：收集所有事件再组装
    events = list(rag.ask_stream(req.question, user_departments=deps, user_id=user_id))
    answer = "".join(e.get("text", "") for e in events if e["type"] == "token")
    sources = next((e["items"] for e in events if e["type"] == "sources"), [])
    trace = next((e["trace"] for e in events if e["type"] == "trace"), {})
    return ChatResponse(answer=answer, sources=sources, trace=trace, trace_id=trace.get("trace_id", ""))
```

- [ ] **Step 4: Run — verify pass**

```powershell
pytest tests/test_api_streaming.py -v
```
Expected: all 5 tests PASS.

- [ ] **Step 5: Commit**

```powershell
git add app/api.py tests/test_api_streaming.py
git commit -m "feat(api): typed SSE chat + sources/trace in ChatResponse"
```

---

### Task 1.4: FastAPI static serving + SPA fallback

**Files:**
- Modify: `app/api.py` (mount `web/dist`, catch-all route — added LAST)

- [ ] **Step 1: Add static serving at the END of `app/api.py`**

Append at the very bottom of `app/api.py` (after all routes):
```python
# ---- 前端静态托管（方案 A：FastAPI 服务 web/dist）----
# 必须放在所有 API 路由之后注册，保证 /api /auth /health 优先匹配。
from fastapi.staticfiles import StaticFiles
from fastapi.responses import FileResponse, HTMLResponse

WEB_DIST = Path(__file__).resolve().parent.parent / "web" / "dist"
_ASSETS = WEB_DIST / "assets"
if _ASSETS.exists():
    app.mount("/assets", StaticFiles(directory=_ASSETS), name="assets")


@app.get("/{full_path:path}")
async def spa_fallback(full_path: str):
    """SPA 路由回退：非 API 路径返回 index.html；dist 未构建时给提示。"""
    if full_path.startswith(("api/", "auth/", "health", "docs", "openapi", "redoc")):
        raise HTTPException(status_code=404)
    index = WEB_DIST / "index.html"
    if index.exists():
        return FileResponse(index)
    return HTMLResponse(
        "<h1>Frontend not built</h1><p>Run: <code>cd web && npm run build</code></p>",
        status_code=404)
```

Note: existing `from fastapi import ... HTTPException` and `from pathlib import Path` are already imported at top of `app/api.py` — reuse them.

- [ ] **Step 2: Manual verify (no dist yet → 404 hint)**

```powershell
.\.venv\Scripts\Activate.ps1
python -c "from app.api import app; print('ok')"
```
Expected: prints `ok` (app imports without error). The catch-all only triggers at request time.

- [ ] **Step 3: Commit**

```powershell
git add app/api.py
git commit -m "feat(api): serve built frontend + SPA fallback"
```

---

### Task 1.5: 硬化（缓存锁 + jwt 校验 + 启动 fail-fast）

**审计 P1**。**Files:** Modify `rag_modules/cache_service.py`（加锁）、`config.py`（validate 查 jwt_secret）、`app/api.py`（lifespan 启动初始化）。

- [ ] **Step 1: CacheService 加锁（TDD）**

Append to `tests/test_api_streaming.py`:
```python
def test_cache_service_is_thread_safe():
    """并发 put 不丢条目、不损坏索引（锁保护下）。"""
    import threading, shutil
    from pathlib import Path
    from rag_modules.cache_service import CacheService
    test_dir = "data/cache_test"
    if Path(test_dir).exists():
        shutil.rmtree(test_dir)
    svc = CacheService(cache_dir=test_dir, embeddings=None)

    def writer(prefix):
        for i in range(50):
            svc.put(f"{prefix}{i}", f"ans-{i}")

    threads = [threading.Thread(target=writer, args=(f"u{n}-",)) for n in range(4)]
    for t in threads: t.start()
    for t in threads: t.join()

    assert svc.stats()["total"] == 200  # 无锁时竞态会 < 200 或抛错
    shutil.rmtree(test_dir, ignore_errors=True)
```

Run: `pytest tests/test_api_streaming.py::test_cache_service_is_thread_safe -v` → FAIL（竞态：total < 200 或 IndexError）。

Implement in `rag_modules/cache_service.py`:
- 顶部 `import threading`。
- `__slots__` 加 `"_lock"`；`__init__` 加 `self._lock = threading.Lock()`。
- `put`、`get`、`clear` 整个方法体用 `with self._lock:` 包裹（`get` 的遍历也在锁内，避免与 `put` 的 append 竞态）。

Run → PASS。

- [ ] **Step 2: `config.validate()` 查 `jwt_secret`**

Modify `config.py`：顶部加 `import logging` + `logger = logging.getLogger(__name__)`；在 `validate()` 返回前加：
```python
        if self.jwt_secret == "rag-dev-secret-change-me-in-production":
            logger.warning("⚠️ JWT_SECRET 仍为默认值——生产务必改成随机长串，否则 token 可伪造")
```
（只 warning 不阻断，避免本地开发被卡。）

- [ ] **Step 3: 启动 fail-fast（FastAPI lifespan）**

Modify `app/api.py`：把 `app = FastAPI(...)` 改为带 lifespan，启动即 `get_rag()`（建索引/下模型），配置缺失此处崩 + 明确日志，而非首请求 500：
```python
from contextlib import asynccontextmanager

@asynccontextmanager
async def lifespan(app: FastAPI):
    get_rag()  # 启动即初始化；LLM_API_KEY 缺失等在此 fail-fast
    yield

app = FastAPI(
    title="企业知识库 RAG 系统",
    description="混合检索 + Reranker + RBAC 权限隔离 + 多级缓存 + 全链路可观测",
    version="1.0.0",
    lifespan=lifespan,
)
```
> 保留 `get_rag()` 懒加载函数不变（lifespan 调它一次后，`_rag` 已就绪，`/health` 等秒回）。注意：lifespan 必须在路由注册前定义 `app`（替换原 `app = FastAPI(...)` 那行）。

- [ ] **Step 4: Run all + commit**

```powershell
cd F:\code\python\GroundRAG
.\.venv\Scripts\Activate.ps1
pytest tests/test_api_streaming.py -v
git add rag_modules/cache_service.py config.py app/api.py tests/test_api_streaming.py
git commit -m "fix: cache lock + jwt_secret validation + startup fail-fast (audit P1)"
```

---

# Phase 2 — Frontend scaffold

### Task 2.1: Scaffold Vite + React + TS + Tailwind

**Files:** Create `web/` via Vite, add Tailwind.

- [ ] **Step 1: Scaffold Vite React-TS**

```powershell
cd F:\code\python\GroundRAG
npm create vite@latest web -- --template react-ts
cd web
npm install
```

- [ ] **Step 2: Install Tailwind + test deps**

```powershell
cd F:\code\python\GroundRAG\web
npm install -D tailwindcss@3 postcss autoprefixer
npm install vitest @testing-library/react @testing-library/jest-dom jsdom @vitejs/plugin-react
npx tailwindcss init -p
```
Note: use Tailwind v3 (stable, well-documented); avoid v4 for this scaffold.

- [ ] **Step 3: Configure Tailwind**

`web/tailwind.config.js`:
```js
/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{ts,tsx}"],
  theme: { extend: {} },
  plugins: [],
}
```

`web/src/styles/index.css` (replace contents):
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

body { margin: 0; }
```

- [ ] **Step 4: Vite config — dev proxy + test config**

`web/vite.config.ts`:
```ts
/// <reference types="vitest" />
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    proxy: {
      '/api': 'http://localhost:8000',
      '/auth': 'http://localhost:8000',
    },
  },
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./src/setupTests.ts'],
  },
})
```

`web/src/setupTests.ts`:
```ts
import '@testing-library/jest-dom'
```

- [ ] **Step 5: Wire `main.tsx` + clean starter**

`web/src/main.tsx`:
```tsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './styles/index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode><App /></React.StrictMode>,
)
```
Delete the Vite default `App.css` and `assets/` if present (we use Tailwind).

- [ ] **Step 6: Commit**

```powershell
cd F:\code\python\GroundRAG
git add web
git commit -m "feat(web): scaffold Vite + React + TS + Tailwind"
```

---

### Task 2.2: Shared types

**Files:** Create `web/src/types.ts`.

- [ ] **Step 1: Write types**

`web/src/types.ts`:
```ts
export interface Source {
  source: string
  page: number | null
  department: string
  score: number
  preview: string
}

export interface TokenUsage { prompt: number; completion: number; total: number }

export interface TraceStep {
  name: string
  ms: number
  tokens?: TokenUsage
}

export interface Trace {
  trace_id: string
  steps: TraceStep[]
  total_ms: number
  tokens: TokenUsage
  cost_usd: number
}

export interface DemoUser {
  user_id: string
  name: string
  departments: string[]
  role: string
}

export type SSEEvent =
  | { type: 'sources'; items: Source[] }
  | { type: 'token'; text: string }
  | { type: 'trace'; trace: Trace }
  | { type: 'error'; message: string }
  | { type: 'done' }
```

- [ ] **Step 2: Commit**

```powershell
git add web/src/types.ts
git commit -m "feat(web): shared TS types"
```

---

# Phase 3 — Frontend data layer

### Task 3.1: SSE chunk parser (TDD — pure function)

**Files:**
- Create: `web/src/api/sse.ts`
- Test: `web/src/api/__tests__/sse.test.ts`

**Interfaces:**
- Produces: `parseSSEChunk(buffer: string): { events: SSEEvent[]; rest: string }`.

- [ ] **Step 1: Write failing test**

`web/src/api/__tests__/sse.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { parseSSEChunk } from '../sse'

describe('parseSSEChunk', () => {
  it('parses complete JSON event frames', () => {
    const buf = 'data: {"type":"token","text":"hi"}\n\n'
    const { events, rest } = parseSSEChunk(buf)
    expect(events).toEqual([{ type: 'token', text: 'hi' }])
    expect(rest).toBe('')
  })

  it('parses multiple frames and keeps the trailing partial', () => {
    const buf = 'data: {"type":"token","text":"a"}\n\ndata: {"type":"token","text":"b"}'
    const { events, rest } = parseSSEChunk(buf)
    expect(events).toEqual([{ type: 'token', text: 'a' }])
    expect(rest).toBe('data: {"type":"token","text":"b"}')
  })

  it('treats [DONE] sentinel as a done event', () => {
    const { events } = parseSSEChunk('data: [DONE]\n\n')
    expect(events).toEqual([{ type: 'done' }])
  })

  it('ignores malformed JSON frames', () => {
    const { events } = parseSSEChunk('data: not-json\n\n')
    expect(events).toEqual([])
  })

  it('parses sources + trace events', () => {
    const buf = 'data: {"type":"sources","items":[{"source":"HR/x.md","page":1,"department":"HR","score":0.9,"preview":"p"}]}\n\n'
    const { events } = parseSSEChunk(buf)
    expect(events[0].type).toBe('sources')
    if (events[0].type === 'sources') expect(events[0].items[0].source).toBe('HR/x.md')
  })
})
```

- [ ] **Step 2: Run — verify fail**

```powershell
cd F:\code\python\GroundRAG\web
npx vitest run src/api/__tests__/sse.test.ts
```
Expected: FAIL (module not found).

- [ ] **Step 3: Implement parser**

`web/src/api/sse.ts`:
```ts
import type { SSEEvent } from '../types'

/**
 * 解析 SSE 文本缓冲：按 "\n\n" 分帧，完整帧解析为事件，最后一帧（可能不完整）作为 rest 返回。
 * 纯函数，无副作用，便于测试。
 */
export function parseSSEChunk(buffer: string): { events: SSEEvent[]; rest: string } {
  const events: SSEEvent[] = []
  const frames = buffer.split('\n\n')
  const rest = frames.pop() ?? ''

  for (const frame of frames) {
    const line = frame.split('\n').find((l) => l.startsWith('data:'))
    if (!line) continue
    const payload = line.slice(5).trim()
    if (payload === '[DONE]') {
      events.push({ type: 'done' })
      continue
    }
    try {
      const obj = JSON.parse(payload)
      if (obj && typeof obj.type === 'string') events.push(obj as SSEEvent)
    } catch {
      // 忽略损坏帧
    }
  }
  return { events, rest }
}
```

- [ ] **Step 4: Run — verify pass**

```powershell
npx vitest run src/api/__tests__/sse.test.ts
```
Expected: 5 tests PASS.

- [ ] **Step 5: Commit**

```powershell
cd F:\code\python\GroundRAG
git add web/src/api/sse.ts web/src/api/__tests__/sse.test.ts
git commit -m "feat(web): SSE chunk parser + tests"
```

---

### Task 3.2: API client + auth hook

**Files:**
- Create: `web/src/api/client.ts`, `web/src/hooks/useAuth.ts`

**Interfaces:**
- Produces: `login(user_id)`, `getDemoUsers()`, `search(question, top_k)`, `getStats()`, `streamChat(question, onEvent, signal)` (uses `parseSSEChunk`).
- `useAuth`: `{ user, token, demoUsers, loginAs, logout }`.

- [ ] **Step 1: Implement client**

`web/src/api/client.ts`:
```ts
import type { DemoUser, Source, SSEEvent, Trace } from '../types'
import { parseSSEChunk } from './sse'

let _token: string | null = localStorage.getItem('groundrag_token')
export function setToken(t: string | null) {
  _token = t
  if (t) localStorage.setItem('groundrag_token', t)
  else localStorage.removeItem('groundrag_token')
}
function authHeaders(): Record<string, string> {
  return _token ? { Authorization: `Bearer ${_token}` } : {}
}

export async function login(user_id: string): Promise<string> {
  const r = await fetch('/auth/login', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ user_id }),
  })
  if (!r.ok) throw new Error(`登录失败: ${r.status}`)
  const data = await r.json()
  setToken(data.access_token)
  return data.access_token
}

export async function getDemoUsers(): Promise<DemoUser[]> {
  const r = await fetch('/auth/demo-users')
  if (!r.ok) return []
  const data = await r.json()
  return data.users
}

export interface SearchResult { source: string; chunk_index: number | null; score: number; preview: string }

export async function search(question: string, top_k = 3): Promise<SearchResult[]> {
  const r = await fetch('/api/v1/search', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...authHeaders() },
    body: JSON.stringify({ question, top_k }),
  })
  if (!r.ok) throw new Error(`检索失败: ${r.status}`)
  const data = await r.json()
  return data.results
}

export async function getStats(): Promise<Record<string, unknown>> {
  const r = await fetch('/api/v1/stats', { headers: authHeaders() })
  if (!r.ok) throw new Error(`统计失败: ${r.status}`)
  return r.json()
}

/**
 * 流式问答：POST /api/v1/chat (stream)，读 ReadableStream，按帧解析，回调每个事件。
 */
export async function streamChat(
  question: string,
  onEvent: (e: SSEEvent) => void,
  signal?: AbortSignal,
): Promise<void> {
  const r = await fetch('/api/v1/chat', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...authHeaders() },
    body: JSON.stringify({ question, stream: true }),
    signal,
  })
  if (!r.ok || !r.body) throw new Error(`问答失败: ${r.status}`)

  const reader = r.body.getReader()
  const decoder = new TextDecoder()
  let buffer = ''
  for (;;) {
    const { done, value } = await reader.read()
    if (done) break
    buffer += decoder.decode(value, { stream: true })
    const { events, rest } = parseSSEChunk(buffer)
    buffer = rest
    for (const e of events) onEvent(e)
  }
}
```

- [ ] **Step 2: Implement `useAuth`**

`web/src/hooks/useAuth.ts`:
```ts
import { useEffect, useState, useCallback } from 'react'
import type { DemoUser } from '../types'
import { login, getDemoUsers } from '../api/client'

export function useAuth() {
  const [demoUsers, setDemoUsers] = useState<DemoUser[]>([])
  const [currentUserId, setCurrentUserId] = useState<string | null>(null)

  useEffect(() => {
    getDemoUsers().then(setDemoUsers).catch(() => {})
  }, [])

  const loginAs = useCallback(async (userId: string | null) => {
    if (!userId) {
      setCurrentUserId(null)
      return
    }
    await login(userId)
    setCurrentUserId(userId)
  }, [])

  const currentUser = demoUsers.find((u) => u.user_id === currentUserId) ?? null
  return { demoUsers, currentUser, loginAs }
}
```

- [ ] **Step 3: Commit**

```powershell
git add web/src/api/client.ts web/src/hooks/useAuth.ts
git commit -m "feat(web): API client + useAuth hook"
```

---

### Task 3.3: `useSSEChat` hook

**Files:** Create `web/src/hooks/useSSEChat.ts`

**Interfaces:**
- Produces: `{ messages, ask, asking }`. Each assistant message = `{ role, content, sources?, trace?, error? }`.

- [ ] **Step 1: Implement hook**

`web/src/hooks/useSSEChat.ts`:
```ts
import { useState, useCallback, useRef } from 'react'
import type { Source, Trace } from '../types'
import { streamChat } from '../api/client'

export interface ChatMessage {
  id: string
  role: 'user' | 'assistant'
  content: string
  sources?: Source[]
  trace?: Trace
  error?: string
}

let _seq = 0
const nextId = () => `m${++_seq}`

export function useSSEChat() {
  const [messages, setMessages] = useState<ChatMessage[]>([])
  const [asking, setAsking] = useState(false)
  const abortRef = useRef<AbortController | null>(null)

  const ask = useCallback(async (question: string) => {
    const userMsg: ChatMessage = { id: nextId(), role: 'user', content: question }
    const assistantId = nextId()
    const assistantMsg: ChatMessage = { id: assistantId, role: 'assistant', content: '' }
    setMessages((m) => [...m, userMsg, assistantMsg])
    setAsking(true)
    const ctrl = new AbortController()
    abortRef.current = ctrl

    const patch = (p: Partial<ChatMessage>) =>
      setMessages((m) => m.map((x) => (x.id === assistantId ? { ...x, ...p } : x)))
    const append = (text: string) =>
      setMessages((m) => m.map((x) => (x.id === assistantId ? { ...x, content: x.content + text } : x)))

    try {
      await streamChat(
        question,
        (e) => {
          if (e.type === 'sources') patch({ sources: e.items })
          else if (e.type === 'token') append(e.text)
          else if (e.type === 'trace') patch({ trace: e.trace })
          else if (e.type === 'error') patch({ error: e.message })
        },
        ctrl.signal,
      )
    } catch (err) {
      patch({ error: (err as Error).message })
    } finally {
      setAsking(false)
    }
  }, [])

  const clear = useCallback(() => setMessages([]), [])
  return { messages, ask, asking, clear }
}
```

- [ ] **Step 2: Commit**

```powershell
git add web/src/hooks/useSSEChat.ts
git commit -m "feat(web): useSSEChat hook"
```

---

# Phase 4 — Frontend views

> Visual styling uses Tailwind utility classes. After functional correctness, invoke the `frontend-design` skill to refine aesthetics (color, spacing, typography) — do not leave it as raw default Tailwind.

### Task 4.1: Chat view + components

**Files:**
- Create: `web/src/components/{UserSwitcher,MessageBubble,SourceCard,TracePanel,ChatWindow}.tsx`
- Create: `web/src/views/ChatView.tsx`

- [ ] **Step 1: `SourceCard`**

`web/src/components/SourceCard.tsx`:
```tsx
import type { Source } from '../types'

export function SourceCard({ s, index }: { s: Source; index: number }) {
  return (
    <div className="border border-slate-200 rounded-md p-2 text-sm bg-slate-50">
      <div className="flex items-center gap-2 mb-1">
        <span className="font-mono text-xs text-slate-500">[文档{index}]</span>
        <span className="font-medium text-slate-700 truncate">{s.source}</span>
        {s.page ? <span className="text-xs text-slate-400">p.{s.page}</span> : null}
        <span className="ml-auto text-xs px-1.5 py-0.5 rounded bg-emerald-100 text-emerald-700">
          {s.department}
        </span>
        <span className="text-xs text-slate-400">score {s.score.toFixed(3)}</span>
      </div>
      <p className="text-slate-600 line-clamp-2">{s.preview}</p>
    </div>
  )
}
```

- [ ] **Step 2: `TracePanel`**

`web/src/components/TracePanel.tsx`:
```tsx
import { useState } from 'react'
import type { Trace } from '../types'

export function TracePanel({ trace }: { trace: Trace }) {
  const [open, setOpen] = useState(false)
  return (
    <div className="mt-2 text-xs">
      <button onClick={() => setOpen((o) => !o)} className="text-slate-500 hover:text-slate-800">
        {open ? '▾' : '▸'} trace · {trace.total_ms.toFixed(0)}ms · tokens {trace.tokens.total} · ≈${trace.cost_usd.toFixed(6)}
      </button>
      {open && (
        <div className="mt-1 font-mono text-[11px] text-slate-600 border-l-2 border-slate-200 pl-2">
          {trace.steps.map((s, i) => (
            <div key={i}>└ {s.name.padEnd(6)} {s.ms.toFixed(1)}ms{s.tokens ? ` tok:${s.tokens.total}` : ''}</div>
          ))}
          <div className="text-slate-400 mt-1">trace_id: {trace.trace_id}</div>
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 3: `MessageBubble`**

`web/src/components/MessageBubble.tsx`:
```tsx
import type { ChatMessage } from '../hooks/useSSEChat'
import { SourceCard } from './SourceCard'
import { TracePanel } from './TracePanel'

export function MessageBubble({ msg }: { msg: ChatMessage }) {
  const isUser = msg.role === 'user'
  return (
    <div className={`flex ${isUser ? 'justify-end' : 'justify-start'}`}>
      <div className={`max-w-[80%] rounded-lg px-3 py-2 ${isUser ? 'bg-blue-600 text-white' : 'bg-white border border-slate-200 text-slate-800'}`}>
        <div className="whitespace-pre-wrap break-words">{msg.content || (msg.error ? '' : '…')}</div>
        {msg.error && <div className="mt-1 text-red-600 text-sm">⚠ {msg.error}</div>}
        {!isUser && msg.sources && msg.sources.length > 0 && (
          <div className="mt-2 space-y-1">
            <div className="text-xs text-slate-500 font-medium">引用来源</div>
            {msg.sources.map((s, i) => <SourceCard key={i} s={s} index={i + 1} />)}
          </div>
        )}
        {!isUser && msg.trace && <TracePanel trace={msg.trace} />}
      </div>
    </div>
  )
}
```

- [ ] **Step 4: `UserSwitcher`**

`web/src/components/UserSwitcher.tsx`:
```tsx
import type { DemoUser } from '../types'

export function UserSwitcher({
  users, current, onSelect,
}: {
  users: DemoUser[]
  current: DemoUser | null
  onSelect: (userId: string | null) => void
}) {
  return (
    <div className="flex items-center gap-2 text-sm">
      <span className="text-slate-500">当前用户:</span>
      <select
        value={current?.user_id ?? ''}
        onChange={(e) => onSelect(e.target.value || null)}
        className="border border-slate-300 rounded px-2 py-1 bg-white"
      >
        <option value="">未登录（全库）</option>
        {users.map((u) => (
          <option key={u.user_id} value={u.user_id}>
            {u.name}（{u.departments.join('/')}）{u.role}
          </option>
        ))}
      </select>
    </div>
  )
}
```

- [ ] **Step 5: `ChatWindow` (input + list)**

`web/src/components/ChatWindow.tsx`:
```tsx
import { useState } from 'react'
import { MessageBubble } from './MessageBubble'
import type { ChatMessage } from '../hooks/useSSEChat'

export function ChatWindow({
  messages, asking, onAsk,
}: {
  messages: ChatMessage[]
  asking: boolean
  onAsk: (q: string) => void
}) {
  const [input, setInput] = useState('')
  const submit = () => {
    const q = input.trim()
    if (!q || asking) return
    onAsk(q)
    setInput('')
  }
  return (
    <div className="flex flex-col h-full">
      <div className="flex-1 overflow-y-auto space-y-3 p-3">
        {messages.length === 0 && (
          <div className="text-slate-400 text-sm p-4 text-center">
            试试问「工作满3年年假几天」「住宿费报销上限」「密码多久换一次」
          </div>
        )}
        {messages.map((m) => <MessageBubble key={m.id} msg={m} />)}
      </div>
      <div className="border-t border-slate-200 p-2 flex gap-2">
        <input
          value={input}
          onChange={(e) => setInput(e.target.value)}
          onKeyDown={(e) => e.key === 'Enter' && submit()}
          placeholder="输入问题…"
          className="flex-1 border border-slate-300 rounded px-3 py-2 outline-none focus:border-blue-500"
        />
        <button
          onClick={submit}
          disabled={asking}
          className="px-4 py-2 rounded bg-blue-600 text-white disabled:bg-slate-300"
        >{asking ? '生成中…' : '发送'}</button>
      </div>
    </div>
  )
}
```

- [ ] **Step 6: `ChatView` (compose)**

`web/src/views/ChatView.tsx`:
```tsx
import { useEffect } from 'react'
import { ChatWindow } from '../components/ChatWindow'
import { UserSwitcher } from '../components/UserSwitcher'
import { useAuth } from '../hooks/useAuth'
import { useSSEChat } from '../hooks/useSSEChat'

export function ChatView() {
  const { demoUsers, currentUser, loginAs } = useAuth()
  const { messages, ask, asking, clear } = useSSEChat()

  // 切换用户时清空对话，避免跨用户串内容
  useEffect(() => { clear() }, [currentUser, clear])

  return (
    <div className="flex flex-col h-[calc(100vh-56px)]">
      <div className="flex items-center justify-between px-3 py-2 border-b border-slate-200 bg-slate-50">
        <UserSwitcher users={demoUsers} current={currentUser} onSelect={loginAs} />
        <span className="text-xs text-slate-400">
          {currentUser ? `可见部门: ${currentUser.departments.join(', ')}` : '未登录 · 全库可见'}
        </span>
      </div>
      <ChatWindow messages={messages} asking={asking} onAsk={ask} />
    </div>
  )
}
```

- [ ] **Step 7: Commit**

```powershell
cd F:\code\python\GroundRAG
git add web/src/components web/src/views/ChatView.tsx
git commit -m "feat(web): chat view with streaming, sources, trace, user switcher"
```

---

### Task 4.2: Search + Stats views

**Files:** Create `web/src/views/SearchView.tsx`, `web/src/views/StatsView.tsx`, `web/src/components/SearchResults.tsx`

- [ ] **Step 1: `SearchResults`**

`web/src/components/SearchResults.tsx`:
```tsx
import type { SearchResult } from '../api/client'

export function SearchResults({ results }: { results: SearchResult[] }) {
  if (results.length === 0) return <div className="text-slate-400 text-sm p-4">无召回</div>
  return (
    <div className="space-y-2 p-3">
      {results.map((r, i) => (
        <div key={i} className="border border-slate-200 rounded p-2 text-sm">
          <div className="flex gap-2 mb-1">
            <span className="font-medium text-slate-700 truncate">{r.source}</span>
            <span className="ml-auto text-xs text-slate-400">score {r.score.toFixed(3)}</span>
          </div>
          <p className="text-slate-600">{r.preview}</p>
        </div>
      ))}
    </div>
  )
}
```

- [ ] **Step 2: `SearchView`**

`web/src/views/SearchView.tsx`:
```tsx
import { useState } from 'react'
import { search, type SearchResult } from '../api/client'
import { SearchResults } from '../components/SearchResults'

export function SearchView() {
  const [q, setQ] = useState('')
  const [results, setResults] = useState<SearchResult[]>([])
  const [loading, setLoading] = useState(false)
  const [err, setErr] = useState('')

  const run = async () => {
    if (!q.trim()) return
    setLoading(true); setErr('')
    try { setResults(await search(q)) }
    catch (e) { setErr((e as Error).message) }
    finally { setLoading(false) }
  }

  return (
    <div className="p-3">
      <div className="flex gap-2 mb-2">
        <input value={q} onChange={(e) => setQ(e.target.value)}
          onKeyDown={(e) => e.key === 'Enter' && run()}
          placeholder="纯检索（不调 LLM）…"
          className="flex-1 border border-slate-300 rounded px-3 py-2 outline-none focus:border-blue-500" />
        <button onClick={run} disabled={loading}
          className="px-4 py-2 rounded bg-slate-700 text-white disabled:bg-slate-300">
          {loading ? '检索中…' : '检索'}
        </button>
      </div>
      {err && <div className="text-red-600 text-sm">⚠ {err}</div>}
      <SearchResults results={results} />
    </div>
  )
}
```

- [ ] **Step 3: `StatsView`**

`web/src/views/StatsView.tsx`:
```tsx
import { useEffect, useState } from 'react'
import { getStats } from '../api/client'

export function StatsView() {
  const [stats, setStats] = useState<Record<string, unknown> | null>(null)
  const [err, setErr] = useState('')

  useEffect(() => {
    getStats().then(setStats).catch((e) => setErr((e as Error).message))
  }, [])

  if (err) return <div className="p-4 text-red-600">⚠ {err}</div>
  if (!stats) return <div className="p-4 text-slate-400">加载中…</div>

  const kb = (stats.knowledge_base ?? {}) as Record<string, unknown>
  const cache = (stats.cache ?? {}) as Record<string, unknown>

  return (
    <div className="p-4 space-y-3 text-sm">
      <div className="grid grid-cols-2 gap-2">
        <Card label="文档数" value={String(kb.total_documents ?? '-')} />
        <Card label="分块数" value={String(kb.total_chunks ?? '-')} />
        <Card label="平均块大小" value={String(Math.round(Number(kb.avg_chunk_size ?? 0)))} />
        <Card label="缓存条目" value={String(cache.total ?? 0)} />
      </div>
      <div>
        <div className="text-slate-500 mb-1">部门分布</div>
        <pre className="bg-slate-50 border border-slate-200 rounded p-2 text-xs">
{JSON.stringify(kb.departments ?? {}, null, 2)}
        </pre>
      </div>
    </div>
  )
}

function Card({ label, value }: { label: string; value: string }) {
  return (
    <div className="border border-slate-200 rounded p-3">
      <div className="text-2xl font-semibold text-slate-800">{value}</div>
      <div className="text-xs text-slate-500">{label}</div>
    </div>
  )
}
```

- [ ] **Step 4: Commit**

```powershell
git add web/src/views/SearchView.tsx web/src/views/StatsView.tsx web/src/components/SearchResults.tsx
git commit -m "feat(web): search + stats views"
```

---

### Task 4.3: App shell (nav) + visual polish

**Files:** Modify `web/src/App.tsx`

- [ ] **Step 1: App shell with nav**

`web/src/App.tsx`:
```tsx
import { useState } from 'react'
import { ChatView } from './views/ChatView'
import { SearchView } from './views/SearchView'
import { StatsView } from './views/StatsView'

type View = 'chat' | 'search' | 'stats'

export default function App() {
  const [view, setView] = useState<View>('chat')
  const tabs: { id: View; label: string }[] = [
    { id: 'chat', label: '💬 问答' },
    { id: 'search', label: '🔍 检索' },
    { id: 'stats', label: '📊 统计' },
  ]
  return (
    <div className="min-h-screen bg-slate-100 text-slate-900">
      <header className="flex items-center gap-4 px-4 h-14 bg-white border-b border-slate-200">
        <h1 className="font-bold text-lg tracking-tight">GroundRAG</h1>
        <span className="text-xs text-slate-400 hidden sm:inline">企业级 RAG 知识库 · 混合检索 + RBAC + 全链路可观测</span>
        <nav className="ml-auto flex gap-1">
          {tabs.map((t) => (
            <button key={t.id} onClick={() => setView(t.id)}
              className={`px-3 py-1.5 rounded text-sm ${view === t.id ? 'bg-blue-600 text-white' : 'text-slate-600 hover:bg-slate-100'}`}>
              {t.label}
            </button>
          ))}
        </nav>
      </header>
      <main>
        {view === 'chat' && <ChatView />}
        {view === 'search' && <SearchView />}
        {view === 'stats' && <StatsView />}
      </main>
    </div>
  )
}
```

- [ ] **Step 2: Build to verify it compiles**

```powershell
cd F:\code\python\GroundRAG\web
npm run build
```
Expected: `dist/` produced, no TS errors.

- [ ] **Step 3: Visual polish (invoke frontend-design)**

Invoke the `frontend-design` skill to refine the look (color system, type scale, chat bubble polish, empty states, responsive layout). Apply its suggestions as Tailwind class edits. Goal: a distinctive, non-templated appearance that reads as professional.

- [ ] **Step 4: Commit**

```powershell
cd F:\code\python\GroundRAG
git add web
git commit -m "feat(web): app shell + visual polish"
```

---

### Task 4.4: i18n（中英双语）接入

**Files:**
- Create: `web/src/i18n/index.ts`
- Modify: `web/src/App.tsx`（包 `LangProvider` + 头部 `LanguageToggle`）
- Modify: 所有组件/视图，把硬编码中文换成 `t('key')`

**Interfaces:**
- Produces: `LangProvider`、`useLang(): { lang, setLang, t }`、`t(key, params?)`、`LanguageToggle`。`Lang = 'zh' | 'en'`，存 localStorage，默认 `'zh'`。

- [ ] **Step 1: 实现 i18n 模块（完整 zh/en 字典）**

`web/src/i18n/index.ts`:
```ts
import { createContext, useContext, useState, useCallback, ReactNode } from 'react'

export type Lang = 'zh' | 'en'

const dict: Record<Lang, Record<string, string>> = {
  zh: {
    subtitle: '企业级 RAG 知识库 · 混合检索 + RBAC + 全链路可观测',
    nav_chat: '💬 问答', nav_search: '🔍 检索', nav_stats: '📊 统计',
    current_user: '当前用户:', not_logged_in: '未登录（全库）',
    visible_depts: '可见部门', no_filter: '未登录 · 全库可见',
    input_placeholder: '输入问题…', send: '发送', generating: '生成中…',
    chat_empty: '试试问「工作满3年年假几天」「住宿费报销上限」「密码多久换一次」',
    sources_label: '引用来源', source_doc: '[文档{n}]',
    search_placeholder: '纯检索（不调 LLM）…', search_btn: '检索', searching: '检索中…',
    no_results: '无召回', stats_loading: '加载中…',
    stat_docs: '文档数', stat_chunks: '分块数', stat_avg: '平均块大小',
    stat_cache: '缓存条目', dept_dist: '部门分布',
  },
  en: {
    subtitle: 'Enterprise RAG · hybrid retrieval + RBAC + full-pipeline observability',
    nav_chat: '💬 Chat', nav_search: '🔍 Search', nav_stats: '📊 Stats',
    current_user: 'User:', not_logged_in: 'Not logged in (all docs)',
    visible_depts: 'Visible depts', no_filter: 'Not logged in · all docs',
    input_placeholder: 'Ask a question…', send: 'Send', generating: 'Generating…',
    chat_empty: 'Try "工作满3年年假几天" / "住宿费报销上限" / "密码多久换一次"',
    sources_label: 'Sources', source_doc: '[Doc {n}]',
    search_placeholder: 'Search only (no LLM)…', search_btn: 'Search', searching: 'Searching…',
    no_results: 'No results', stats_loading: 'Loading…',
    stat_docs: 'Documents', stat_chunks: 'Chunks', stat_avg: 'Avg chunk size',
    stat_cache: 'Cache entries', dept_dist: 'Department distribution',
  },
}

interface Ctx { lang: Lang; setLang: (l: Lang) => void; t: (key: string, p?: Record<string, string | number>) => string }
const LangContext = createContext<Ctx | null>(null)

export function LangProvider({ children }: { children: ReactNode }) {
  const [lang, setLangState] = useState<Lang>(() => (localStorage.getItem('groundrag_lang') as Lang) || 'zh')
  const setLang = useCallback((l: Lang) => { setLangState(l); localStorage.setItem('groundrag_lang', l) }, [])
  const t = useCallback((key: string, p?: Record<string, string | number>) => {
    let s = dict[lang][key] ?? key
    if (p) for (const k of Object.keys(p)) s = s.replace(`{${k}}`, String(p[k]))
    return s
  }, [lang])
  return <LangContext.Provider value={{ lang, setLang, t }}>{children}</LangContext.Provider>
}

export function useLang() {
  const ctx = useContext(LangContext)
  if (!ctx) throw new Error('useLang must be used within LangProvider')
  return ctx
}

export function LanguageToggle() {
  const { lang, setLang } = useLang()
  return (
    <div className="flex gap-1 text-xs">
      <button onClick={() => setLang('zh')} className={lang === 'zh' ? 'font-bold text-slate-900' : 'text-slate-400'}>中</button>
      <span className="text-slate-300">/</span>
      <button onClick={() => setLang('en')} className={lang === 'en' ? 'font-bold text-slate-900' : 'text-slate-400'}>EN</button>
    </div>
  )
}
```

- [ ] **Step 2: App 包 Provider + 头部加 LanguageToggle + 用 `t()`**

替换 `web/src/App.tsx`：
```tsx
import { useState } from 'react'
import { ChatView } from './views/ChatView'
import { SearchView } from './views/SearchView'
import { StatsView } from './views/StatsView'
import { LangProvider, useLang, LanguageToggle } from './i18n'

type View = 'chat' | 'search' | 'stats'

function Shell() {
  const { t } = useLang()
  const [view, setView] = useState<View>('chat')
  const tabs: { id: View; key: string }[] = [
    { id: 'chat', key: 'nav_chat' }, { id: 'search', key: 'nav_search' }, { id: 'stats', key: 'nav_stats' },
  ]
  return (
    <div className="min-h-screen bg-slate-100 text-slate-900">
      <header className="flex items-center gap-4 px-4 h-14 bg-white border-b border-slate-200">
        <h1 className="font-bold text-lg tracking-tight">GroundRAG</h1>
        <span className="text-xs text-slate-400 hidden sm:inline">{t('subtitle')}</span>
        <div className="ml-auto flex items-center gap-3">
          <LanguageToggle />
          <nav className="flex gap-1">
            {tabs.map((tb) => (
              <button key={tb.id} onClick={() => setView(tb.id)}
                className={`px-3 py-1.5 rounded text-sm ${view === tb.id ? 'bg-blue-600 text-white' : 'text-slate-600 hover:bg-slate-100'}`}>
                {t(tb.key)}
              </button>
            ))}
          </nav>
        </div>
      </header>
      <main>
        {view === 'chat' && <ChatView />}
        {view === 'search' && <SearchView />}
        {view === 'stats' && <StatsView />}
      </main>
    </div>
  )
}

export default function App() {
  return <LangProvider><Shell /></LangProvider>
}
```

- [ ] **Step 3: 各组件/视图把硬编码中文换成 `t()`**

每个文件 `import { useLang } from '../i18n'`，顶部 `const { t } = useLang()`，按下表替换（`t()` 必须在 `LangProvider` 内的组件里调用）：

| 文件 | 原中文 | 替换为 |
|---|---|---|
| UserSwitcher | `当前用户:` | `t('current_user')` |
| UserSwitcher | `未登录（全库）` | `t('not_logged_in')` |
| ChatView | `可见部门:` | `t('visible_depts')` |
| ChatView | `未登录 · 全库可见` | `t('no_filter')` |
| ChatWindow | placeholder `输入问题…` | `t('input_placeholder')` |
| ChatWindow | `生成中…` / `发送` | `t('generating')` / `t('send')` |
| ChatWindow | 空态提示整句 | `t('chat_empty')` |
| MessageBubble | `引用来源` | `t('sources_label')` |
| SourceCard | `[文档{index}]` | `t('source_doc', { n: index })` |
| SearchView | placeholder / 按钮 / 加载 | `t('search_placeholder')` / `t('search_btn')` / `t('searching')` |
| SearchResults | `无召回` | `t('no_results')` |
| StatsView | `加载中…` | `t('stats_loading')` |
| StatsView | `文档数`/`分块数`/`平均块大小`/`缓存条目` | `t('stat_docs')`/`t('stat_chunks')`/`t('stat_avg')`/`t('stat_cache')` |
| StatsView | `部门分布` | `t('dept_dist')` |

> 数据内容（部门名、文档名、LLM 回答、用户姓名、score/page/trace 数字）**不翻译**，只翻译 UI 框架文案。

- [ ] **Step 4: 构建验证**

```powershell
cd F:\code\python\GroundRAG\web
npm run build
```
Expected: 无 TS 错误。手动切 中/EN，所有 UI 文案随之切换。

- [ ] **Step 5: Commit**

```powershell
cd F:\code\python\GroundRAG
git add web/src/i18n web/src/App.tsx web/src/components web/src/views
git commit -m "feat(web): bilingual UI (zh/en) with lightweight i18n"
```

---

# Phase 5 — Integration & one-command run

### Task 5.1: Makefile + end-to-end run

**Files:** Create `Makefile` at repo root.

- [ ] **Step 1: Write Makefile**

`F:\code\python\GroundRAG\Makefile`:
```makefile
.PHONY: setup dev build run test web-test

setup:
	python -m venv .venv
	.venv\Scripts\activate && pip install -U pip && pip install -r requirements.txt
	cd web && npm install

# 开发：两个进程（后端 reload + 前端 vite）
dev:
	start "groundrag-api" cmd /c ".venv\Scripts\activate && uvicorn app.api:app --reload --port 8000"
	cd web && npm run dev

# 构建前端
build:
	cd web && npm run build

# 一键运行：构建前端 + 单服务托管
run: build
	.venv\Scripts\activate && uvicorn app.api:app --port 8000

test:
	.venv\Scripts\activate && pytest -v
	cd web && npx vitest run
```

- [ ] **Step 2: End-to-end manual run (prod mode)**

```powershell
cd F:\code\python\GroundRAG
.\.venv\Scripts\Activate.ps1
# 确认 .env 有真实 LLM_API_KEY
npx --prefix web vite build   # or: cd web; npm run build
uvicorn app.api:app --port 8000
```
Open `http://localhost:8000`. Expected: GroundRAG UI loads. Switch to admin, ask "工作满3年年假几天" → streaming answer + sources citing `HR/年假管理制度.md` + trace panel with steps/tokens/cost. Switch to 张三(HR), ask "住宿费报销上限" → should refuse / not find (finance doc not in HR scope).

- [ ] **Step 3: Commit**

```powershell
git add Makefile
git commit -m "feat: Makefile (setup/dev/build/run/test)"
```

---

# Phase 6 — Bilingual README + LICENSE

### Task 6.1: README (EN primary) + README.zh-CN.md + LICENSE

**Files:** Create `README.md`, `README.zh-CN.md`, `LICENSE`.

- [ ] **Step 1: `LICENSE` (MIT)**

Standard MIT license text, copyright `2026 GroundRAG`. (Use the canonical MIT template.)

- [ ] **Step 2: `README.md` (English)**

`F:\code\python\GroundRAG\README.md`:
````markdown
[English](README.md) | [中文](README.zh-CN.md)

# GroundRAG

> An enterprise-grade Retrieval-Augmented Generation knowledge base — hybrid retrieval, RBAC document isolation, multi-level cache, full-pipeline observability, and a React UI you can run with one command.

GroundRAG is a portfolio-grade RAG showcase. Ask questions against a company knowledge base and get **grounded answers with source citations** — every fact links back to the document, page, and department it came from. Different users see different documents (RBAC), and a built-in tracer shows per-step latency, token usage, and cost for every answer.

![architecture](docs/architecture.png) <!-- add screenshot/diagram after build -->

## ✨ Features

- **Hybrid retrieval** — vector (bge-small-zh) + BM25 (jieba), fused with RRF, de-duplicated with MMR, optional bge-reranker.
- **RBAC document isolation** — per-department FAISS sub-indexes; an HR user physically cannot retrieve finance documents.
- **Grounded answers** — anti-hallucination prompt + `[文档X]` citations rendered as source cards.
- **Full-pipeline observability** — a self-built tracer reports intent-routing / rewrite / retrieval / generation timing, token usage, and estimated cost per query.
- **Multi-provider LLM with fallback** — OpenAI-compatible (DeepSeek, GLM, Qwen, Ollama) + Anthropic; auto-failover with circuit-breaker cooldown.
- **Multi-level cache** — exact + semantic, permission-isolated, with a 4-rule safety check to prevent false hits.
- **React UI** — streaming chat, source cards, trace panel, search-only mode, stats dashboard.

## 🏗 Architecture

```
User question
  → cache lookup (permission-signed)
  → intent routing (shortcut for clear queries)
  → query rewrite (shortcut for clear queries)
  → permission-aware retrieval (RBAC sub-index)
  → streaming generation (anti-hallucination + citations)
  → store cache
  (every step traced → trace.jsonl)
```

Frontend (React + Vite + TypeScript + Tailwind) talks to FastAPI over typed SSE events (`sources` → `token` → `trace` → `done`). In production FastAPI serves the built frontend, so one `uvicorn` process serves everything.

## 🚀 Quick Start

**Prerequisites:** Python 3.12–3.13, Node.js ≥ 18, an LLM API key (e.g. DeepSeek).

```bash
git clone https://github.com/<you>/GroundRAG.git
cd GroundRAG
cp .env.example .env          # then edit .env and set LLM_API_KEY
make setup                    # create venv + install deps + npm install
make run                      # build frontend + start server
```

Open http://localhost:8000 — pick a user in the chat header (try `admin`), and ask **"工作满3年年假几天"**.

> Dev mode (hot reload): `make dev` starts both the API (`:8000`) and Vite (`:5173`).

## 🧪 Testing

```bash
make test                     # backend pytest + frontend vitest
```

## ⚙️ Configuration

All knobs are environment variables (see `.env.example`): chunk size, retrieval `k`s, RRF `k`, score thresholds, LLM provider/key/model, JWT secret, Langfuse (optional).

## 📂 Project Layout

```
app/            FastAPI (auth, api, static serving)
rag_modules/    data prep, index, retrieval, generation, cache, tracer, llm factory/fallback
web/            React + Vite frontend
data/docs/      synthetic sample knowledge base (fictional company)
evaluation/     LLM-as-judge eval, A/B rerank comparison, retrieval sweep
tests/          pytest
```

## ⚠️ Known Limitations (intentional for this showcase)

- **Passwordless login** — demo accounts authenticate by `user_id` only (rate-limited). A real deployment adds password verification before JWT issuance.
- **Reranker off by default** — bge-reranker crashes on some Windows/CPU setups (`0xC0000005`); it's available in `evaluation/` but disabled in the live path. RRF + MMR is used instead. **Note: the eval/A-B scripts do use the reranker, so their numbers reflect rerank-on and are NOT directly comparable to the live (rerank-off) retrieval.**
- **Token/cost figures are estimates** — token accounting relies on the streaming `include_usage` path; behavior varies across providers and the fallback chain, so treat trace cost as approximate.
- **Security boundary** — the FAISS index uses pickle deserialization (`allow_dangerous_deserialization`). It only loads locally-generated index files; never feed it an index from an untrusted source.
- **Same-origin only** — the app is served same-origin (FastAPI hosts the built frontend), so no CORS is configured. If you deploy the frontend on a separate origin, add `CORSMiddleware` yourself.
- **Sample docs are synthetic** — a fictional company; swap `data/docs/` for your own.

## 📄 License

MIT
````

- [ ] **Step 3: `README.zh-CN.md` (Chinese — same structure, translated)**

Translate the full `README.md` into natural Chinese. Same sections, same commands. Top line: `[English](README.md) | [中文](README.zh-CN.md)`. Key feature bullets translated (混合检索 / RBAC 权限隔离 / 防幻觉 + 引用溯源 / 全链路可观测 / 多供应商降级链 / 多级缓存). Architecture diagram caption in Chinese. Quick Start identical commands with Chinese narration. Known limitations translated (无密码登录 / reranker 默认关闭 / 合成样例文档).

- [ ] **Step 4: Commit**

```powershell
cd F:\code\python\GroundRAG
git add README.md README.zh-CN.md LICENSE
git commit -m "docs: bilingual README + LICENSE"
```

---

### Task 6.2: Architecture diagram + screenshots placeholders

**Files:** `docs/architecture.png` (add after build), capture 2–3 screenshots.

- [ ] **Step 1: Capture screenshots**

Run the app (`make run`), capture: (1) Chat with answer + source cards + trace open, (2) RBAC isolation (HR can't see finance), (3) Stats dashboard. Save under `docs/img/`. Reference them in README (replace the placeholder).

- [ ] **Step 2: Commit**

```powershell
git add docs/img
git commit -m "docs: screenshots"
```

---

# Phase 7 — Containerization & deployment

### Task 7.1: 多阶段 Dockerfile + .dockerignore

**Files:** Create `Dockerfile`, `.dockerignore`.

- [ ] **Step 1: Dockerfile（多阶段：node 构前端 → python 运行时）**

`F:\code\python\GroundRAG\Dockerfile`:
```dockerfile
# ---- Stage 1: build frontend ----
FROM node:20-alpine AS web-build
WORKDIR /web
COPY web/package*.json ./
RUN npm ci
COPY web/ ./
RUN npm run build

# ---- Stage 2: runtime ----
FROM python:3.12-slim
WORKDIR /app
RUN apt-get update && apt-get install -y --no-install-recommends git curl \
 && rm -rf /var/lib/apt/lists/*
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
# 审计 P1：构建期预下载 embedding 模型 → 镜像自包含，首请求秒回（不依赖运行时联网/下模型）
ENV HF_HOME=/app/.cache/huggingface \
    SENTENCE_TRANSFORMERS_HOME=/app/.cache/sentence-transformers
RUN python -c "from langchain_huggingface import HuggingFaceEmbeddings; HuggingFaceEmbeddings(model_name='BAAI/bge-small-zh-v1.5', model_kwargs={'device':'cpu'})"
COPY app/ ./app/
COPY rag_modules/ ./rag_modules/
COPY config.py main.py locustfile.py ./
COPY tests/ ./tests/
COPY scripts/ ./scripts/
COPY data/docs/ ./data/docs/
COPY data/users.json ./data/users.json
COPY --from=web-build /web/dist ./web/dist
ENV OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 \
    HF_HUB_OFFLINE=1
# 审计 P1：非 root 运行（app 用户拥有 /app，可写 data/{vector_index,cache,traces}）
RUN useradd -m -r app && chown -R app /app
USER app
EXPOSE 8000
CMD ["uvicorn", "app.api:app", "--host", "0.0.0.0", "--port", "8000"]
```

- [ ] **Step 2: .dockerignore**

`F:\code\python\GroundRAG\.dockerignore`:
```
.git
.venv
.env
__pycache__
*.pyc
web/node_modules
web/dist
data/cache
data/traces
data/vector_index
.pytest_cache
.idea
.vscode
```

- [ ] **Step 3: Commit**

```powershell
cd F:\code\python\GroundRAG
git add Dockerfile .dockerignore
git commit -m "feat: multi-stage Dockerfile + .dockerignore"
```

---

### Task 7.2: docker-compose + 部署文档 + README Docker 节

**Files:** Create `docker-compose.yml`, `docs/deployment.md`; modify `README.md` / `README.zh-CN.md`（加 Docker 节）。

- [ ] **Step 1: docker-compose.yml**

`F:\code\python\GroundRAG\docker-compose.yml`:
```yaml
services:
  groundrag:
    build: .
    container_name: groundrag
    ports:
      - "8000:8000"
    env_file:
      - .env
    # embedding 模型已在镜像构建期预下载（见 Dockerfile），无需运行时联网或模型卷。
    # 若构建机国内拉 HF 慢：build 前 export HF_ENDPOINT=https://hf-mirror.com 再 docker compose build。
    volumes:
      - rag-index:/app/data/vector_index
      - rag-cache:/app/data/cache
      - rag-traces:/app/data/traces
    restart: unless-stopped

volumes:
  rag-index:
  rag-cache:
  rag-traces:
```

> `data/docs/` 与 `data/users.json` 来自镜像（不挂载，避免空卷遮蔽样例）。索引/缓存/traces 挂命名卷持久化；embedding 模型已烘焙进镜像（无需卷、首请求不卡）。

- [ ] **Step 2: 部署文档 `docs/deployment.md`**

内容（中文为主）：前置（Docker + Compose）；`cp .env.example .env` 并填 `LLM_API_KEY`、改 `JWT_SECRET`；`docker compose up --build`（**构建期**已下载 bge 模型并烘焙进镜像，容器首启无需联网等模型；日志出现"嵌入模型初始化完成"后即就绪）；访问 http://localhost:8000；卷说明（索引/缓存/traces 命名卷持久化，重建容器不重建索引；embedding 模型来自镜像；`data/docs`+`users.json` 来自镜像）；容器以非 root 用户 `app` 运行；镜像体积约 1.5–2GB；常见问题（构建期 HF 慢→build 前 `export HF_ENDPOINT=https://hf-mirror.com`；端口冲突→改 compose `ports`；运行时模型已在镜像内且 `HF_HUB_OFFLINE=1`，不再联网拉模型）。

- [ ] **Step 3: README 双语各加 "🐳 Docker" 节**

`README.md`（Quick Start 之后）：
```markdown
## 🐳 Docker

```bash
cp .env.example .env          # set LLM_API_KEY
docker compose up --build     # embedding model is pre-baked into the image at build time
```
Open http://localhost:8000. See [docs/deployment.md](docs/deployment.md).
```
`README.zh-CN.md` 对应位置插入中文版（同命令，中文说明）。

- [ ] **Step 4: 端到端验证**

```powershell
cd F:\code\python\GroundRAG
docker compose up --build
```
Expected: 容器启动，日志显示模型加载（来自镜像，秒级）+ 索引构建；浏览器 :8000 可用、问答正常。停后再 `docker compose up`（不带 --build）应秒起（索引命中卷，模型已在镜像）。

- [ ] **Step 5: Commit**

```powershell
cd F:\code\python\GroundRAG
git add docker-compose.yml docs/deployment.md README.md README.zh-CN.md
git commit -m "feat: docker-compose + deployment doc + README docker section"
```

---

# Phase 8 — Final verification

### Task 8.1: Acceptance checklist

- [ ] **Step 1: Clean repo check**

```powershell
cd F:\code\python\GroundRAG
git log --oneline         # fresh history, no old commits
Test-Path .env            # True locally, but:
git ls-files | findstr /I "\.env$"   # must be empty (.env not tracked)
git ls-files | findstr /I "mye"      # must be empty (no real company docs)
```
Expected: `.env` not tracked; no `mye` docs tracked.

- [ ] **Step 2: Full test suite**

```powershell
.\.venv\Scripts\Activate.ps1
pytest -v
cd web; npx vitest run; cd ..
```
Expected: all backend + frontend tests PASS.

- [ ] **Step 3: Acceptance criteria walk-through**

Verify each (from spec §14):
- [ ] `make run` → browser opens UI
- [ ] Chat: streaming + source cards + trace panel work
- [ ] Switch HR↔财务↔admin reproduces RBAC isolation
- [ ] Search / Stats views return data
- [ ] pytest + vitest green
- [ ] Bilingual README complete with quickstart
- [ ] Bilingual UI (中/EN toggle) switches all framework strings
- [ ] `docker compose up --build` → :8000 usable; index/cache/traces volumes persist across container recreations; embedding model pre-baked (first request fast)
- [ ] Audit hardening: `ask()`/`ask_stream()` share helpers; streaming `/chat` doesn't block event loop; `CacheService` locked; `validate()` warns on default `JWT_SECRET`; Docker runs non-root; missing config fails fast at startup

- [ ] **Step 4: Ready to push**

When the user creates the empty GitHub repo `GroundRAG`:
```powershell
cd F:\code\python\GroundRAG
git remote add origin https://github.com/<you>/GroundRAG.git
git branch -M main
git push -u origin main
```

---

## Self-Review (completed during authoring)

**Spec coverage:** §1-2 goal/name ✓ (Task 0.1, 6.1); §3 features ✓ (Tasks 1.x, 4.x); §4 YAGNI ✓ (Task 6.1 limitations); §5 repo structure ✓ (Task 0.1, file structure); §6 backend ✓ (Tasks 1.1-1.4); §7 frontend ✓ (Tasks 2-4); §8 data hygiene ✓ (Task 0.2); §9 errors ✓ (sse.ts malformed-frame ignore, ask_stream error event, ChatView error bubble); §10 testing ✓ (Tasks 1.1-1.3 pytest, 3.1 vitest); §11 README ✓ (Task 6.1); §12 migration ✓ (Phase 0); §14 acceptance ✓ (Task 7.1).

**Placeholder scan:** All steps contain concrete code/commands. README screenshot references are explicit "capture after build" actions, not vague TODOs. LICENSE uses canonical MIT (standard text).

**Type consistency:** `Source`/`Trace`/`DemoUser`/`SSEEvent` defined in Task 2.2, used identically in 3.1/3.2/3.3/4.x. Backend `_source_item` fields (`source,page,department,score,preview`) match frontend `Source`. `ask_stream` event shapes match `parseSSEChunk` expectations. `ChatResponse` fields (`answer,sources,trace,trace_id`) match the non-stream test + frontend. i18n dictionary keys (Task 4.4) match the `t()` calls in the component mapping table. Dockerfile copies `scripts/` (used by Task 0.2 to generate `data/docs/`) — `scripts/` must exist before the image build; Task 0.2 creates it.

**Late-added scope coverage:** §7.6 bilingual UI ✓ (Task 4.4); §14 Docker ✓ (Phase 7, Task 7.1–7.2); multi-format sample docs (md/txt/xlsx/docx/pdf) ✓ (Task 0.2 via `scripts/make_sample_docs.py`, run in Task 0.3). Acceptance is Task 8.1.

**Audit hardening coverage (spec §16):** P0 DRY ✓ (Task 1.2 helpers `_decide_intent/_rewrite_query/_retrieve` used by both `ask()` and `ask_stream()`); P1 event-loop non-blocking ✓ (Task 1.3 thread→`asyncio.Queue`); P1 cache lock ✓ (Task 1.5 Step 1); P1 jwt validation ✓ (Task 1.5 Step 2); P1 non-root Docker ✓ (Task 7.1 Dockerfile); P1 fail-fast startup ✓ (Task 1.5 Step 3 lifespan); P1 model pre-baked ✓ (Task 7.1 Dockerfile RUN + compose drops model-cache volume); P2 README notes ✓ (Task 6.1 Known Limitations).
